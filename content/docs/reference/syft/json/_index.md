+++
title = "JSON Schema"
weight = 18
menu_group = "syft"
# we don't really have useful content here, lets get the user started instead
# note that this is an alias link, so we cannot use relref hugo features
manualLink = "/docs/reference/syft/json/latest"
+++
